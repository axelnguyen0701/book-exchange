type ExampleEntity @entity {
  id: ID!
  count: BigInt!
  owner: Bytes! # address
  approved: Bytes! # address
}

type Listing @entity {
  id: ID!
  tokenId: BigInt!
  seller: Bytes! # address
  owner: Bytes! # address
  instantPrice: BigInt!
  startingPrice: BigInt!
  allowBid: Boolean!
  sold: Boolean!
  bidList: [Bid!]! @derivedFrom(field: "listing")
}

type Bid @entity {
  id: ID!
  listing: Listing!
  bidder: Bytes! # address
  amount: BigInt!
}

